# Scarlett Task: Hermes Desktop UI Surface

## One bounded objective

Inventory the user-visible features implemented in:

`C:\Users\clsor\Documents\Codex\HermesAgent\source\apps\desktop`

Write the result immediately and incrementally to:

`C:\Users\clsor\Documents\Codex\HermesAgent\docs\research\scarlett-hermes-functionality-inventory.md`

That report is the only file you may create or modify.

## Do not do these things

- Do not inspect `web_server.py`; another agent owns backend mapping.
- Do not scan the whole repository.
- Do not install, build, run, format, configure, or change anything.
- Do not create temporary files or scripts.
- Do not reread this assignment after you begin.
- Do not restart the investigation if output is truncated. Continue from your notes.

## Procedure

1. Create the report now with the headings below.
2. Inspect only `source\apps\desktop` using file listing and targeted text searches.
3. Update the report after each category so partial work is preserved.
4. Stop after a maximum of 40 source-file reads. Mark unknowns instead of expanding scope.

## Required report headings

1. `# Hermes Desktop UI Functionality Inventory`
2. `## Entry Points and Navigation`
3. `## Chat and Message Features`
4. `## Sessions and History`
5. `## Tools, MCP, and Approvals`
6. `## Files, Terminal, and Artifacts`
7. `## Models, Providers, and Settings`
8. `## Keyboard Shortcuts and Commands`
9. `## States and Errors`
10. `## Recommended Parity Order`
11. `## Backend Questions for the Adapter Pass`

For each feature, include the exact repo-relative source path and symbol/component when available. Label evidence as `Confirmed` or `Uncertain`. Use `P0`, `P1`, or `P2` priority. Never guess.

Finish by verifying the report exists at the exact output path and state that no other file was changed.
